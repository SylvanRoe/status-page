# CF Pages 自定义响应头（0823-sp-3 部署缓存策略）
# 背景：style.css/app.js 默认 max-age=14400 + ETag REVALIDATED，部署后用户 4 小时内仍看缓存旧版
#       （「明暗切换没 icon / 语言下拉平铺」均因此）。
# 策略：页面静态资源改 no-cache(must-revalidate) → 浏览器每次刷新条件请求，内容变更立即生效；
#       index.html 里资源 URL 另带 ?v=<sha>（workflow 注入），双保险绕过浏览器+边缘缓存。
/style.css
  Cache-Control: public, max-age=0, must-revalidate
/app.js
  Cache-Control: public, max-age=0, must-revalidate
